---
title: "WitnessOps Architecture"
description: "Clean system overview for how WitnessOps connects governed execution, proof production, and portable verification."
section: "docs"
order: 10
nav_label: "WitnessOps Architecture"
draft: false
---

# WitnessOps Architecture

WitnessOps architecture is the boundary map that connects governed runtime control to portable proof and later independent review.

## 1. Problem this page solves

Without a shared architecture map, readers collapse execution and proof into one surface and overstate what either layer can guarantee.

This page defines the end-to-end system flow and ownership boundaries before the deeper layer and posture pages.

## 2. What you should understand after reading

After this page, you should understand:

- the end-to-end flow from intent to external verification
- which responsibilities belong to runtime control vs proof production
- what the architecture preserves and what remains outside system guarantees

## 3. Mechanism-first architecture path

```text
Intent + authority context
  -> Governed execution path
    -> Evidence capture
      -> Signed continuity artifacts
        -> Portable bundle
          -> Independent verification
```

Runtime control governs what can happen: scope, approval, and runbook-constrained execution.
Proof production begins at evidence capture and binds what did happen into portable artifacts for later review.

Inputs to this path are authority context, scoped intent, and runtime outputs.
Outputs are signed receipts, continuity links, and bundle artifacts that outside reviewers can verify without runtime access.

### Responsibility boundaries

| Responsibility | What it owns | Primary depth page |
| --- | --- | --- |
| Runtime control | policy, approvals, scope, governed execution | [Governed Execution](/docs/security-systems/governed-execution) |
| Layer ownership model | separation of contracts, runtime, and proof | [Three-Layer Stack](/docs/security-systems/three-layer-stack) |
| Proof artifact construction | receipts, chains, bundle material | [Receipts](/docs/evidence/receipts) |
| Independent checking | verifier procedure and verdict semantics | [Verification](/docs/how-it-works/verification) |

## 4. Observed vs inferred

| Layer | What is explicit | What remains interpretation |
| --- | --- | --- |
| Observed | documented runtime controls, proof-artifact path, verification interfaces | none beyond documented behavior |
| Inferred | confidence in operational trust posture from architecture quality | depends on reviewer policy and real implementation integrity |

## 5. Trust assumptions

Architecture reduces ambiguity, but it does not remove outside dependencies:

- scanner and tool correctness remains external
- host and runtime integrity remains external
- upstream identity, scope, and policy input quality remains external
- legal interpretation of evidence remains external

## 6. Next-page handoff

Next, read [Three-Layer Stack](/docs/security-systems/three-layer-stack) to inspect the ownership separation that this architecture map depends on.

Then use [Governed Execution](/docs/security-systems/governed-execution) and [Threat Model](/docs/security-systems/threat-model) for runtime and boundary depth.
