---
title: "Receipts"
description: "Conceptual bridge page for what a WitnessOps receipt is, what it proves, and how it relates to bundles, signatures, timestamps, and chain continuity."
section: "docs"
order: 2
nav_label: "Receipts"
draft: false
---

# Receipts

A receipt is the atomic proof object in WitnessOps. It is the smallest portable statement about a governed event that can be checked independently.

## 1. Problem this page solves

Security teams often share logs and screenshots, but those are hard to verify independently.

Receipts solve a narrower and stronger problem:

- make a governed event portable
- bind that event to issuer-authenticated cryptographic material
- make tampering and continuity breaks detectable

## 2. What you should understand after reading

After this page, you should understand:

- what a receipt contains at conceptual level
- what receipts prove vs what they do not prove
- how receipts relate to bundles, timestamps, and chain continuity
- why receipt strength is layered rather than binary

## 3. Receipt mechanism (what it is)

A receipt combines four elements:

1. statement about a governed action/decision/result
2. issuer-bound signature material
3. continuity linkage to surrounding receipt stream
4. references to supporting proof artifacts in the bundle

In practice, it tells a reviewer what happened, under whose recorded authority, and where this event sits in sequence.

## 4. What a receipt proves

When verification succeeds for available layers, a receipt can prove:

- the issuer-bound statement has not been modified
- the statement exists at a specific place in continuity history
- referenced proof layers (timestamp/inclusion/witness) are satisfied when present and valid

This is stronger than logging and narrower than claiming end-to-end operational truth.

## 5. What remains outside receipt proof

A receipt does not prove:

- underlying tool correctness
- perfect operator/approver judgment
- uncompromised host state by itself
- complete story if surrounding artifacts are missing

Receipt proof is about governed issuance integrity, not omniscience.

## 6. Strength ladder (layered assurance)

| Level | Meaning |
| --- | --- |
| Signed receipt | Issuer claim can be checked |
| Timestamped receipt | Signed object is bound to trusted time |
| Bundled receipt | Surrounding integrity material is available for offline checks |
| Witnessed receipt | Declared witness/trust policy checks also succeed |

Each level is additive. Missing higher layers means "proves less," not "worthless."

## 7. Common interpretation and failure signals

Verification focuses on concrete checks: required fields, hashes, signatures, digest relationships, and policy-declared trust layers.

Typical failure signals:

- hash mismatch -> bytes changed
- signature failure -> issuer authenticity not established
- digest mismatch -> proof references do not align
- `indeterminate` -> required external trust condition not established locally

Report by layer, not with a vague single score.

## 8. Next-page handoff

Next, read [Receipt Specification](/docs/evidence/receipt-spec) for the field-level technical contract.

Then continue to [Execution Chains](/docs/evidence/execution-chains) for continuity and ordering behavior across multiple receipts.
