export function RouteScrollReset() {
  // Next.js and the browser own route scrolling, hash landings, and history
  // restoration. This compatibility shim deliberately makes no scroll writes.
  return null;
}
