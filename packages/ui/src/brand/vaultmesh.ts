import { witnessops, type WitnessOpsBrand } from "./witnessops";

// Legacy alias kept for compatibility. The active brand object is WitnessOps.
export const vaultmesh = witnessops;

export type VaultMeshBrand = WitnessOpsBrand;
