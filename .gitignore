node_modules/
.next/
.next-dev/
dist/
.turbo/
.env
.env.local
.env.*.local
*.tsbuildinfo
.vercel
.DS_Store
.claude/*
!.claude/commands/
!.claude/commands/**
# run-<unit> skills are committed agent tooling (driver.mjs + SKILL.md)
!.claude/skills/
!.claude/skills/**
.playwright-mcp/
tmp/
out/
output/
/artifacts/ui-proof/

# Screenshot / audit artifacts
audit-*.png
*-cards.png
.playwright-cli

# run-witnessops-web driver runtime outputs (screenshots, dev log, pidfile)
var/screenshots/
var/driver-dev.log
var/smoke-run.log
var/.driver-dev.pid

# Azure Developer CLI local environment (contains live env identifiers / secrets)
.azure/

# Local Bicep parameters file (contains live resource names). Use main.parameters.example.json as template.
infra/main.parameters.json

# Bicep compiled output (build artifact)
infra/main.json
infra/modules/*.json
