apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Template metadata only. Deploy with apply.sh after private rendering.
namespace: ${DEPLOY_NS}
resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
images:
  - name: witnessops-web
    newName: ghcr.io/witnessops/witnessops-web
    newTag: replace-me
