apiVersion: v1
kind: Service
metadata:
  name: witnessops-web
  namespace: witnessops
  labels:
    app: witnessops-web
spec:
  type: ClusterIP
  selector:
    app: witnessops-web
  ports:
    - name: http
      port: 3000
      targetPort: 3000