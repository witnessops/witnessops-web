# witnessops-web runtime environment (historical Compose example)
#
# Copy this to the private WITNESSOPS_WEB_ENV_FILE, fill in real secret values,
# and apply restrictive ownership/mode. This file is the
# env_file referenced by deploy/docker-compose.yml.
#
# Keys below mirror the live edge02 runtime set. Directory values point at the
# in-container mount targets from docker-compose.yml (/data/*), NOT host paths.
#
# Never commit a filled-in copy of this file.

# --- Core ---
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0

# --- Public origin / verification ---
NEXT_PUBLIC_OS_SITE_URL=https://witnessops.com
WITNESSOPS_VERIFY_BASE_URL=https://witnessops.com

# --- Mail (Resend transactional; Proton handles human inboxes) ---
WITNESSOPS_MAIL_PROVIDER=resend
WITNESSOPS_RESEND_API_KEY=re_replace_me
# WITNESSOPS_RESEND_WEBHOOK_SECRET=whsec_replace_me
# Dedicated secret for the separate verification/MFA delivery webhook endpoint.
# WITNESSOPS_RESEND_VERIFICATION_WEBHOOK_SECRET=whsec_replace_me
# Optional trusted normalized response-outcome adapter. Keep it separate from
# the mailbox receipt secret and from provider-native webhook secrets.
# WITNESSOPS_RESPONSE_OUTCOME_SECRET=replace-with-response-outcome-secret
# WITNESSOPS_MAILBOX_RECEIPT_SECRET=replace-with-mailbox-receipt-secret
# Transactional verification/MFA sender. This is an outbound-only Resend subdomain.
WITNESSOPS_TOKEN_FROM_EMAIL=noreply@send.witnessops.com
WITNESSOPS_MAILBOX_ENGAGE=engage@witnessops.com
WITNESSOPS_MAILBOX_SUPPORT=support@witnessops.com
WITNESSOPS_MAILBOX_NOREPLY=noreply@send.witnessops.com
# Admin Inbox reconciliation only. Keep the exported authorized_user JSON in
# BASE_ENV_SECRET; the runtime materializes a mode-0600 file for each gws call.
# WITNESSOPS_GWS_CREDENTIALS_JSON=
# Only set in file mail mode (non-production). Maps to the /data/mail-out mount.
# WITNESSOPS_MAIL_OUTPUT_DIR=/data/mail-out

# --- Tokens ---
WITNESSOPS_TOKEN_SIGNING_SECRET=replace-with-long-random-secret
WITNESSOPS_CLAIMANT_SESSION_SECRET=replace-with-long-random-secret
WITNESSOPS_TOKEN_TTL_MINUTES=30

# --- Persistent storage (in-container targets; host mounts in compose) ---
WITNESSOPS_INTAKE_STORE_DIR=/data/intake-store
WITNESSOPS_TOKEN_STORE_DIR=/data/intake-store
WITNESSOPS_INTAKE_EVENT_DIR=/data/intake-events
WITNESSOPS_TOKEN_AUDIT_DIR=/data/intake-events

# --- Optional: docs assistant (disable unless explicitly provisioned) ---
# WITNESSOPS_DOCS_ASSISTANT_ENABLED=false
# WITNESSOPS_DOCS_ASSISTANT_STAGE=staging
# WITNESSOPS_DOCS_ASSISTANT_MODEL=
# WITNESSOPS_DOCS_ASSISTANT_VECTOR_STORE_ID=
# OPENAI_API_KEY=

# --- Optional: public Ask WitnessOps OpenAI path (explicit production gate) ---
# Reuses the exact docs-assistant model/vector-store allowlist above. Keep the
# API key server-side in approved secret custody; never use a NEXT_PUBLIC key.
# WITNESSOPS_ASK_OPENAI_ENABLED=false
# WITNESSOPS_ASK_OPENAI_STAGE=production

# --- Optional: control plane / governed exposure (leave unset to disable) ---
# CONTROL_PLANE_URL=
# CONTROL_PLANE_API_KEY=
# GES_SERVER_URL=
# GES_ASSESSMENT_KEY=

# --- Optional: admin routes (only if enabled) ---
# Required for Google transaction and admin-session signing.
# WITNESSOPS_ADMIN_SECRET=
# Production runtime role. Supported values: Founder, Delegated Operator, Administrator.
WITNESSOPS_ADMIN_ROLE=Founder

# Google Workspace OIDC for named admin access. These values are server-only.
# Keep them in the separately custodied ADMIN_OIDC_SECRET Kubernetes object.
# Production redirect: https://witnessops.com/api/admin/google/callback
# WITNESSOPS_GOOGLE_OIDC_CLIENT_ID=replace-with-google-oauth-client-id
# WITNESSOPS_GOOGLE_OIDC_CLIENT_SECRET=replace-with-google-oauth-client-secret
# WITNESSOPS_GOOGLE_OIDC_REDIRECT_URI=https://witnessops.com/api/admin/google/callback
# WITNESSOPS_GOOGLE_WORKSPACE_DOMAIN=example.com
# WITNESSOPS_GOOGLE_ADMIN_EMAIL_ALLOWLIST=operator@example.com,backup-operator@example.com
