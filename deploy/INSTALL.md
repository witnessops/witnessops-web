# Historical host installation note

Status: `HISTORICAL_PUBLIC_STUB`

This file records that `witnessops-web` previously had a Docker Compose / GHCR host-installation path. That path is historical and is not current production authority.

The public repository intentionally does not retain the old host path setup, credential-file permissions, registry-login commands, Caddy host commands, or direct deploy/rollback procedure as current documentation.

Public boundary:

- Historical deployment source may remain for reconstruction, but it must not be used as a production shortcut.
- Current production state and deployment authority require current release-specific evidence and separately authorized operator procedures.
- Host paths, credentials, backup locations, and direct rollback commands belong in restricted non-public operator custody.
- Reopening a retired Compose/GHCR deployment path requires a separate explicit review and authority decision.

The exact historical source remains reconstructable from Git history.
